---
- name: AIDAS EC2 기본 설정
  hosts: all
  become: true

  tasks:
    - name: 연결 확인
      ping:

# FastAPI는 blue ASG 인스턴스 user_data(compute.tf)에서 실행
# - name: FastAPI 컨테이너 배포
#   hosts: all
#   become: true
#   roles:
#     - ec2_fastapi
